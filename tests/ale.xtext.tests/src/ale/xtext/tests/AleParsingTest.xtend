/*
 * generated by Xtext 2.12.0
 */
package ale.xtext.tests

import ale.xtext.ale.AleRoot
import com.google.inject.Inject
import org.eclipse.xtext.testing.InjectWith
import org.eclipse.xtext.testing.XtextRunner
import org.eclipse.xtext.testing.util.ParseHelper
import org.junit.Assert
import org.junit.Test
import org.junit.runner.RunWith

@RunWith(XtextRunner)
@InjectWith(AleInjectorProvider)
class AleParsingTest {
	@Inject
	ParseHelper<AleRoot> parseHelper
	
	@Test
	def void loadModel() {
		val result = parseHelper.parse('''
			behavior fsmprinting
			
			import ecore "platform:/resource/minifsm/model/MiniFsm.ecore"
			
			open class Machine {
				def String print() {
					return self.states.map[s|alg.$(s).print()].join("\n")
				}
			}
			
			open class State {
			
				def String print() {
					return "State " + self.name + "\n" + self.out.map[t|"\t" + alg.$(t).print()].join("\n")
				}
			}
			
			open class FinalState {
				override String print() {
					return "* " + alg.state(self).print();
				}
			}
			
			open class Transition {
				def String print() {
					return self.event + " => " + self.tgt.name
				}
			}

		''')
		Assert.assertNotNull(result)
		Assert.assertTrue(result.eResource.errors.isEmpty)
	}
}
