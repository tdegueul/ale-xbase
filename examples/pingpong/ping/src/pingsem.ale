behavior pingsem

import ecore "platform:/resource/ping/model/ping.ecore"

open class Ping {
	def void call() {
		println('Ping')
		if (obj.cptr > 0) {
			obj.cptr = obj.cptr - 1
			alg.$(obj.pong).call(obj.cptr)
		}
	}
	
}

open abstract class Pong {
	abstract def void call(Integer value)
}